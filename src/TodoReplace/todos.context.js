import React from 'react';
import * as STATUS from './status.constants';

const todoList = [

];

const updateTodo = () => {};

const TodoContext = React.createContext({todoList, updateTodo});

export default TodoContext;
